spring.application.name=adquirencia-core
# Usa el puerto que asigne la plataforma (Railway/Render) y 8080 en local.
server.port=${PORT:8080}
spring.jackson.serialization.write-dates-as-timestamps=false

# Login compartido: usuario/contrasena desde variables de entorno con defaults locales.
app.username=${APP_USERNAME:admin}
app.password=${APP_PASSWORD:paguelofacil}
